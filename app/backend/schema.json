// users
{
  "user_id": "string",
  "ethAddress": "string",
  "art": [
    {
      "id": "string", // art_id from instagram
      "nft_id": "string"
    }
  ]
}

// bid orders
{
  "makerAddress": "string",
  
}